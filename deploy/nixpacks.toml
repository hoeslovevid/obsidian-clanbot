# Nixpacks build plan (path set via railway.toml → nixpacksConfigPath).
# Fallback for services still on the Nixpacks builder.
[variables]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"
NIXPACKS_PYTHON_VERSION = "3.11.11"

[phases.install]
cmds = ["pip install -r deploy/requirements.txt"]
